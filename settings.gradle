pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)  // Измените на PREFER_SETTINGS
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "JoyConVirtualGamepad"
include ':app'
